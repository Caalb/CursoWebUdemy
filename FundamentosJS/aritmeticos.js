const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d;
const subtracao = d - b;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2 // Este módulo é para ver se tem resto da divisão no número (muito usando para descobrir se um número é par)

console.log(soma, subtracao, multiplicacao, divisao, modulo)